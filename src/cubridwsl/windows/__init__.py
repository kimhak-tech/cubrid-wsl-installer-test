"""The machine in PARTS, on the Windows side.

One module per thing you would ask about: the product's registry key, its
Apps & Features entry, its Tray process, its files on disk. Each module owns
its whole surface -- what you can READ there and what you can DO there -- so
the module you call is the route you are testing.
"""
